# Sample PyDeck Plugin Repo

This folder is a ready-to-upload example of a marketplace plugin repository layout.

## Structure

- `manifest.json` contains the full plugin index.
- `plugins/<plugin-slug>/icon.svg` is the icon path referenced in the manifest.
- `plugins/<plugin-slug>/<plugin-version>/` contains the installable files for that version.
